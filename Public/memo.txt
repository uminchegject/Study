//floatの場合
for(int i = 0; i < data_byte_list.size(); ++4)
{
	float data_float;
	std::memcpy(&float, data_byte_list[i], sizeof(float));
	std::count << "data_float :" << float << std::end;
}

//intの場合
for(int i = 0; i < data_byte_list.size(); ++4)
{
	int32_t data_int32_t;
	std::memcpy(&int, data_byte_list[i], sizeof(int32_t));
	std::count << "data_int32_t :" << data_int32_t << std::end;
}
